'''
`kobo.apps.subsequences` --as in Sub(mission)Sequences is an app for defining
and following a sequence of actions or changes to a submission that has come
into kobo.

models:
- SubmissionData:
      Holds a JSONField with the "supplementalData" necessary to complete the

tasks:
(things that are queued in celery for later action)

needs writeup:
    - how to develop / debug within this app
    - description of tests

'''
