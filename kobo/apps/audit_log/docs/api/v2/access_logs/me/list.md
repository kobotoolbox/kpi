### Creates an export task for current user

The metadata field will always return the `auth-type` field but `source` and `ip_address` are only returned when the
`auth-type` is equal to `submission-group`.
