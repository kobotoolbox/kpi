def run():
    """
    Deprecated: the trench MFA library was removed, so this migration no longer
    has anything to do. The file is kept because migration
    `long_running_migrations.0013_migrate_mfa_data` still registers this job on
    every new instance.
    """

    pass
