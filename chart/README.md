# How to add an env var to a PR env
To add a environment variable to the ephemeral PR environment, add the value to `chart/pr_envs.yml`.  From there it will be added to all deployments(kpi,workers, etc).
