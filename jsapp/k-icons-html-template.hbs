<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{fontName}}</title>
  <style>
    body {
      font-size: 20px;
      margin: 2rem;
    }
    
    .preview-table {
      border-collapse: collapse;
    }

    .preview-table th,
    .preview-table td {
      border-bottom: 1px solid hsl(0, 0%, 80%);
      padding: 0.5rem;
      line-height: 1;
    }

    .preview-table th {
      text-align: left;
      border-bottom-width: 2px;
    }
    
    .preview-table th.icon,
    .preview-table td.icon {
      text-align: center;
    }
    
    .preview-table td.icon {
      font-size: 2rem;
      padding: 0;
    }
    
    .preview-table td.code {
      font-family: monospace;
      background: hsl(0, 0%, 90%);
    }
    
    .preview-table pre {
      margin: 0;
    }
    
    .k-icon {
      display: inline-block;
      vertical-align: top;
    }
    
    code {
      background: hsl(0, 0%, 90%);
      padding-left: 0.5rem;
      padding-right: 0.5rem;
      border-radius: 0.25rem;
    }

    {{{styles}}}
  </style>
</head>
<body>
  <h1>{{fontName}}</h1>
  
  <p>Note: using the icons require importing a React component <code>import Icon from 'js/components/common/icon'</code>. You can also set a size with <code>size=</code> attribute, but it's optional.</p>

  <table class="preview-table">
    <tr>
      <th>name</th>
      <th class="icon">icon</th>
      <th>JSX code</th>
    </tr>
    
    {{#each names}}
    <tr>
      <td class="name">{{this}}</td>
      <td class="icon">
        <i class="{{../baseClassName}} {{../classPrefix}}{{this}}"></i>
      </td>
      <td class="code">
        <pre><code>&lt;Icon name='{{this}}'/&gt;</code></pre>
      </td>
    </tr>
    {{/each}}
  </table>
</body>
</html>
