# Generated by Django 4.2.15 on 2024-08-18 11:38

from django.db import migrations, models
import kpi.fields.file
import kpi.models.asset_file


def populate_created_by(apps, schema_editor):
    Asset = apps.get_model('kpi', 'Asset')
    Invite = apps.get_model('project_ownership', 'Invite')

    for asset in Asset.objects.all():
        created_by = None

        if asset.owner:
            created_by = asset.owner.username

            # Check if the asset was transferred
            first_invite = Invite.objects.filter(
                recipient=asset.owner,
            ).order_by('date_created').first()

            if first_invite:
                created_by = first_invite.sender.username

        Asset.objects.filter(id=asset.id).update(created_by=created_by)



class Migration(migrations.Migration):

    dependencies = [
        ('kpi', '0057_assetexportsettings_date_created_and_more'),
        ('project_ownership', '__first__'),
    ]

    operations = [
        migrations.AddField(
            model_name='asset',
            name='created_by',
            field=models.CharField(blank=True, db_index=True, max_length=150, null=True),
        ),
        migrations.AddField(
            model_name='asset',
            name='last_modified_by',
            field=models.CharField(blank=True, db_index=True, max_length=150, null=True),
        ),
        migrations.RunPython(populate_created_by),
    ]
