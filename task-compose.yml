# WIP services for testing dockerfiles on this branch.
# Not intended for merge into main.

services:
  mk:
    build:       {  context: ., dockerfile: task-Dockerfile }
    networks:    [ mk-network ]
  
  mk-node:
    build:       {  context: ., dockerfile: task-node-Dockerfile }
    networks:    [ mk-network ]

networks: 
  mk-network:
    driver: bridge
